<!DOCTYPE html>
<html lang="es">

<head>
    <%- include('../partials/head') %>
        <title>DevStyle | <%= product.name %>
        </title>
</head>

<body>

    <%- include('../partials/header') %>

        <main class="detail-container">

            <div class="breadcrumb">
                <a href="/">Inicio</a> <i class="fas fa-chevron-right"></i>
                <span style="text-transform: capitalize;">
                    <%= product.category %>
                </span> <i class="fas fa-chevron-right"></i>
                <span class="current">
                    <%= product.name %>
                </span>
            </div>

            <section class="product-detail">

                <div class="detail-image">
                    <img src="/img/<%= product.image %>" alt="<%= product.name %>">
                </div>

                <div class="detail-info">
                    <h1>
                        <%= product.name %>
                    </h1>

                    <div class="price-box">
                        <span class="final-price">$<%= product.price %></span>
                        <% if (product.discount> 0) { %>
                            <span class="discount">
                                <%= product.discount %>% OFF
                            </span>
                            <% } %>
                    </div>

                    <p class="payment-method"><i class="far fa-credit-card"></i> 12 cuotas sin interés</p>

                    <p class="description">
                        <%= product.description %>
                    </p>

                    <form action="/products/cart" method="GET" class="buy-form">
                        <div class="selectors">
                            <div class="selector-group">
                                <label>Color:</label>
                                <select name="color">
                                    <option>Estándar</option>
                                    <option>Alternativo</option>
                                </select>
                            </div>
                            <div class="selector-group">
                                <label>Cantidad:</label>
                                <input type="number" name="quantity" value="1" min="1" max="5">
                            </div>
                        </div>

                        <button type="submit" class="btn-primary" style="width: 100%;">AGREGAR AL CARRITO</button>
                    </form>
                    <div class="admin-actions" style="margin-top: 20px; border-top: 1px solid #333; padding-top: 20px;">
                        <h4 style="color: #666; margin-bottom: 10px; font-size: 0.9rem;">Panel de Administración:</h4>

                        <a href="/products/edit/<%= product.id %>" class="btn-edit" style="margin-bottom: 10px;">
                            <i class="fas fa-edit"></i> EDITAR PRODUCTO
                        </a>

                        <form action="/products/<%= product.id %>?_method=DELETE" method="POST"
                            onsubmit="return confirm('⚠️ ¿Estás seguro de que quieres eliminar este producto? Esta acción no se puede deshacer.')">

                            <button type="submit" class="btn-delete">
                                <i class="fas fa-trash-alt"></i> ELIMINAR PRODUCTO
                            </button>
                        </form>
                    </div>

                    <div class="security-info">
                        <p><i class="fas fa-shield-alt"></i> Garantía oficial de 12 meses</p>
                        <p><i class="fas fa-truck"></i> Envío gratis a todo el país</p>
                    </div>

                </div>
            </section>
        </main>

        <%- include('../partials/footer') %>

</body>

</html>