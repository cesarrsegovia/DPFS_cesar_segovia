<!DOCTYPE html>
<html lang="es">

<head>
    <%- include('../partials/head') %>
        <title>DevStyle | <%= product.name %>
        </title>
</head>

<body>

    <%- include('../partials/header') %>

        <main class="detail-container">

            <div class="breadcrumb">
                <a href="/">Inicio</a> <i class="fas fa-chevron-right"></i>
                <span style="text-transform: capitalize;">
                    <%= product.category %>
                </span> <i class="fas fa-chevron-right"></i>
                <span class="current">
                    <%= product.name %>
                </span>
            </div>

            <section class="product-detail">

                <div class="detail-image">
                    <img src="/img/<%= product.image %>" alt="<%= product.name %>">
                </div>

                <div class="detail-info">
                    <h1>
                        <%= product.name %>
                    </h1>

                    <div class="price-box">
                        <span class="final-price">$<%= product.price %></span>

                        <% if (product.discount> 0) { %>
                            <span class="discount">
                                <%= product.discount %>% OFF
                            </span>
                            <% } %>
                    </div>

                    <p class="payment-method"><i class="far fa-credit-card"></i> 12 cuotas sin interés</p>

                    <p class="description">
                        <%= product.description %>
                    </p>

                    <form action="/products/cart" class="buy-form">
                        <div class="selectors">
                            <div class="selector-group">
                                <label>Color:</label>
                                <select>
                                    <option>Estándar</option>
                                    <option>Alternativo</option>
                                </select>
                            </div>
                            <div class="selector-group">
                                <label>Cantidad:</label>
                                <input type="number" value="1" min="1" max="5">
                            </div>
                        </div>

                        <div class="actions">
                            <button type="button" class="btn-primary" style="width: 100%;">AGREGAR AL CARRITO</button>
                        </div>
                    </form>

                    <div class="security-info">
                        <p><i class="fas fa-shield-alt"></i> Garantía oficial de 12 meses</p>
                        <p><i class="fas fa-truck"></i> Envío gratis a todo el país</p>
                    </div>
                </div>
            </section>
        </main>

        <%- include('../partials/footer') %>

</body>

</html>