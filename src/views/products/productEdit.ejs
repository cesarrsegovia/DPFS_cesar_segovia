<!DOCTYPE html>
<html lang="es">

<head>
    <%- include('../partials/head') %>
</head>

<body>

    <%- include('../partials/header') %>

        <main class="auth-container">
            <section class="auth-box" style="max-width: 800px;">
                <h2>Editar Producto 📦</h2>

                <form action="/products/<%= product.id %>?_method=PUT" method="POST" enctype="multipart/form-data">

                    <div style="display: flex; gap: 20px; flex-wrap: wrap;">
                        <div class="form-group" style="flex: 2;">
                            <label for="name">Nombre del Producto</label>
                            <div class="input-wrapper">
                                <i class="fas fa-tag"></i>
                                <input type="text" id="name" name="name" value="<%= product.name %>" required>
                            </div>
                        </div>

                        <div class="form-group" style="flex: 1;">
                            <label for="price">Precio</label>
                            <div class="input-wrapper">
                                <i class="fas fa-dollar-sign"></i>
                                <input type="number" id="price" name="price" value="<%= product.price %>" step="0.01"
                                    required>
                            </div>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="description">Descripción</label>
                        <textarea name="description" id="description" rows="4"
                            style="width: 100%; padding: 10px; background: #2c2c2c; border: none; color: white; border-radius: 5px;"><%= product.description %></textarea>
                    </div>

                    <div style="display: flex; gap: 20px; flex-wrap: wrap;">
                        <div class="form-group" style="flex: 1;">
                            <label for="category">Categoría</label>
                            <div class="input-wrapper">
                                <i class="fas fa-list"></i>
                                <select name="category" id="category"
                                    style="width: 100%; background: transparent; border: none; color: white; outline: none;">
                                    <option value="perifericos" <%=product.category=='perifericos' ? 'selected' : '' %>
                                        >Periféricos</option>
                                    <option value="ropa" <%=product.category=='ropa' ? 'selected' : '' %>>Ropa</option>
                                    <option value="libros" <%=product.category=='libros' ? 'selected' : '' %>>Libros
                                    </option>
                                    <option value="accesorios" <%=product.category=='accesorios' ? 'selected' : '' %>
                                        >Accesorios</option>
                                </select>
                            </div>
                        </div>

                        <div class="form-group" style="flex: 1;">
                            <label>Imagen Actual: <span style="color:var(--accent);">
                                    <%= product.image %>
                                </span></label>
                            <div class="input-wrapper">
                                <i class="fas fa-camera"></i>
                                <input type="file" id="image" name="image" accept="image/*">
                            </div>
                        </div>
                    </div>

                    <div style="margin-top: 20px;">
                        <button type="submit" class="btn-primary">ACTUALIZAR PRODUCTO</button>
                    </div>
                </form>
            </section>
        </main>

        <%- include('../partials/footer') %>

</body>

</html>